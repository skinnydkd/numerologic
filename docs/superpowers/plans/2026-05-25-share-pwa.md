# Compartir + PWA (Pla 2c) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Afegir el compartir resultat (estil Wordle, sense desvelar solucions) i convertir el client en una PWA instal·lable i offline-first.

**Architecture:** El text de compartir es genera amb `share.js` (pur, testable). El botó de compartir usa `navigator.share` amb caiguda a porta-retalls. La PWA s'aconsegueix amb `manifest.json` + un service worker `sw.js` cache-first que precarrega l'app shell i `data/puzzles.json`. S'enforteix la lectura de `localStorage` corrupte.

**Tech Stack:** JavaScript (ES modules), `node:test`, Web App Manifest, Service Worker API. Sense build ni dependències.

**Spec de referència:** `docs/superpowers/specs/2026-05-25-share-pwa-design.md`.

**Prerequisit:** El Pla 2b (PR #3) ha d'estar fusionat a `main`. Treballar en una branca nova `feat/share-pwa` sortida de `main`.

---

## File Structure

```
numerologic/
  manifest.json           # CREAR: web app manifest
  sw.js                   # CREAR: service worker (cache-first)
  icon.svg                # CREAR: icona mini-rusc
  index.html              # MODIFICAR: <head> PWA + botó "Comparteix"
  js/
    share.js              # CREAR: buildShareText, emojiBar
    app.js                # MODIFICAR: handler de compartir + registre del SW
    storage.js            # MODIFICAR: loadProgress robust
    game.js               # MODIFICAR: restauració robusta
    tests/
      share.test.js       # CREAR
      storage.test.js     # MODIFICAR (test de valor corrupte)
      game.test.js        # MODIFICAR (test de progrés corrupte)
```

---

## Task 1: `share.js` — text de compartir

**Files:**
- Create: `js/share.js`
- Test: `js/tests/share.test.js`

- [ ] **Step 1: Escriure els tests que fallen**

`js/tests/share.test.js`:

```javascript
import { test } from "node:test";
import assert from "node:assert/strict";
import { emojiBar, buildShareText } from "../share.js";

test("emojiBar buit", () => {
  assert.equal(emojiBar(0, 118), "⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜");
});

test("emojiBar ple", () => {
  assert.equal(emojiBar(118, 118), "🟦🟦🟦🟦🟦🟦🟦🟦🟦🟦");
});

test("emojiBar parcial", () => {
  assert.equal(emojiBar(87, 118), "🟦🟦🟦🟦🟦🟦🟦🟨⬜⬜");
});

test("buildShareText sense tutti", () => {
  assert.equal(
    buildShareText({ number: 12, rankName: "Expert", found: 87, total: 118, tuttiFound: false }),
    "Numerològic #12 · Expert\n87/118 · 🟦🟦🟦🟦🟦🟦🟦🟨⬜⬜"
  );
});

test("buildShareText amb tutti", () => {
  assert.equal(
    buildShareText({ number: 12, rankName: "Geni", found: 118, total: 118, tuttiFound: true }),
    "Numerològic #12 · Geni\n118/118 · 🟦🟦🟦🟦🟦🟦🟦🟦🟦🟦\n★ Tutti!"
  );
});
```

- [ ] **Step 2: Executar per verificar que fallen**

Run: `node --test js/tests/share.test.js`
Expected: FAIL (`Cannot find module '../share.js'`).

- [ ] **Step 3: Implementar `js/share.js`**

```javascript
// Genera el text de compartir (estil Wordle). Mai inclou cap solució.

export function emojiBar(found, total, cells = 10) {
  const ratio = total > 0 ? found / total : 0;
  let filled = Math.floor(ratio * cells);
  if (filled > cells) filled = cells;
  const partial = ratio * cells - filled > 0 && filled < cells ? 1 : 0;
  const empty = cells - filled - partial;
  return "🟦".repeat(filled) + "🟨".repeat(partial) + "⬜".repeat(empty);
}

export function buildShareText({ number, rankName, found, total, tuttiFound }) {
  const lines = [
    `Numerològic #${number} · ${rankName}`,
    `${found}/${total} · ${emojiBar(found, total)}`,
  ];
  if (tuttiFound) lines.push("★ Tutti!");
  return lines.join("\n");
}
```

- [ ] **Step 4: Executar per verificar que passen**

Run: `node --test js/tests/share.test.js`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add js/share.js js/tests/share.test.js
git commit -m "feat: share.js (text de compartir estil Wordle)"
```

---

## Task 2: Robustesa de `localStorage` corrupte

**Files:**
- Modify: `js/storage.js`
- Modify: `js/game.js`
- Modify: `js/tests/storage.test.js`
- Modify: `js/tests/game.test.js`

- [ ] **Step 1: Afegir els tests que fallen**

A `js/tests/storage.test.js`, afegir aquest test al final del fitxer (no toquis els existents):

```javascript
test("valor corrupte -> null (no llança)", () => {
  const store = fakeStore();
  store.setItem("numerologic:p:9", "{no json");
  assert.equal(loadProgress(store, 9), null);
});
```

A `js/tests/game.test.js`, afegir aquest test al final del fitxer (no toquis els existents):

```javascript
test("progrés corrupte no peta (omet entrades dolentes)", () => {
  const g = createGame(puzzle(), { found: [{ canonical: "x", text: "@@@" }], tuttiFound: false });
  assert.equal(g.found.size, 0);
});
```

- [ ] **Step 2: Executar per verificar que fallen**

Run: `node --test js/tests/storage.test.js js/tests/game.test.js`
Expected: FAIL — `storage` llança `SyntaxError` (JSON.parse del valor corrupte) i `game` llança `ParseError` (parse de `"@@@"`).

- [ ] **Step 3: Endurir `js/storage.js`**

A `js/storage.js`, substituir la funció `loadProgress` sencera per:

```javascript
export function loadProgress(store, poolIndex) {
  const raw = store.getItem(PREFIX + poolIndex);
  if (!raw) return null;
  try {
    return JSON.parse(raw);
  } catch {
    return null;
  }
}
```

- [ ] **Step 4: Endurir `js/game.js`**

A `js/game.js`, dins de `createGame`, substituir el bloc de restauració:

```javascript
  if (savedProgress) {
    for (const f of savedProgress.found || []) {
      const ast = parse(f.text);
      found.set(f.canonical, {
        text: f.text,
        points: solutionPoints(countLeaves(ast), hasPowOrSqrt(ast)),
      });
    }
    tuttiFound = Boolean(savedProgress.tuttiFound);
  }
```

per:

```javascript
  if (savedProgress) {
    for (const f of savedProgress.found || []) {
      let ast;
      try {
        ast = parse(f.text);
      } catch {
        continue; // omet entrades corruptes en lloc de bloquejar la partida
      }
      found.set(f.canonical, {
        text: f.text,
        points: solutionPoints(countLeaves(ast), hasPowOrSqrt(ast)),
      });
    }
    tuttiFound = Boolean(savedProgress.tuttiFound);
  }
```

(Si el nom del paràmetre del teu `game.js` és `savedProgress`, açò encaixa exactament; és el nom que es va fixar al Pla 2b.)

- [ ] **Step 5: Executar per verificar que passen**

Run: `node --test js/tests/storage.test.js js/tests/game.test.js`
Expected: PASS (storage 4, game 11).

- [ ] **Step 6: Commit**

```bash
git add js/storage.js js/game.js js/tests/storage.test.js js/tests/game.test.js
git commit -m "fix: localStorage corrupte no bloqueja el joc"
```

---

## Task 3: Botó "Comparteix"

**Files:**
- Modify: `index.html`
- Modify: `js/app.js`

- [ ] **Step 1: Afegir el botó a `index.html`**

A `index.html`, dins del `<div class="meta">`, substituir:

```html
        · <span class="star" id="tutti">★ Tutti pendent</span>
        <span id="practiceNew" class="hidden"> · <button class="rankbtn" id="newPractice">Nova pràctica</button></span>
```

per:

```html
        · <span class="star" id="tutti">★ Tutti pendent</span>
        · <button class="rankbtn" id="share">Comparteix</button>
        <span id="practiceNew" class="hidden"> · <button class="rankbtn" id="newPractice">Nova pràctica</button></span>
```

- [ ] **Step 2: Afegir l'import a `js/app.js`**

A `js/app.js`, després de la línia `import * as ui from "./ui.js";`, afegir:

```javascript
import { buildShareText } from "./share.js";
```

- [ ] **Step 3: Registrar l'element `share`**

A `js/app.js`, dins de l'objecte `els`, afegir `share: $("share"),` (per exemple, just després de `panel: $("panel"),`).

- [ ] **Step 4: Afegir el handler de compartir**

A `js/app.js`, a la secció `// Events` (per exemple després del listener de `els.newPractice`), afegir:

```javascript
els.share.addEventListener("click", async () => {
  const text = buildShareText({
    number: poolIndex + 1,
    rankName: game.rank(),
    found: game.found.size,
    total: game.puzzle.solutions.length,
    tuttiFound: game.tuttiFound,
  });
  try {
    if (navigator.share) {
      await navigator.share({ text });
    } else {
      await navigator.clipboard.writeText(text);
      els.flash.className = "flash found";
      els.flash.textContent = "Copiat!";
    }
  } catch {
    // l'usuari ha cancel·lat o no s'ha pogut compartir; ignora
  }
});
```

- [ ] **Step 5: Verificar sintaxi i servir**

Run: `node --check js/app.js`
Expected: cap sortida.

Run: `python -m http.server 8000` i obrir `http://localhost:8000`. Comprovar que el botó "Comparteix" surt al peu i que en clicar-lo (a escriptori) apareix el flaix "Copiat!" i el text és al porta-retalls (enganxa'l en algun lloc per veure el format). Aturar amb Ctrl+C.

- [ ] **Step 6: Commit**

```bash
git add index.html js/app.js
git commit -m "feat: boto Comparteix (navigator.share o porta-retalls)"
```

---

## Task 4: `manifest.json` + `<head>` PWA + `icon.svg`

**Files:**
- Create: `manifest.json`
- Create: `icon.svg`
- Modify: `index.html`

- [ ] **Step 1: Crear `manifest.json`**

```json
{
  "name": "Numerològic",
  "short_name": "Numerològic",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#58b4c4",
  "icons": [
    { "src": "icon.svg", "sizes": "any", "type": "image/svg+xml", "purpose": "any maskable" }
  ]
}
```

- [ ] **Step 2: Crear `icon.svg` (mini-rusc)**

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <rect width="512" height="512" rx="96" fill="#ffffff"/>
  <g fill="#58b4c4">
    <polygon points="192.4,75.8 253,110.8 253,180.8 192.4,215.8 131.8,180.8 131.8,110.8"/>
    <polygon points="319.6,75.8 380.2,110.8 380.2,180.8 319.6,215.8 259,180.8 259,110.8"/>
    <polygon points="128.8,186 189.4,221 189.4,291 128.8,326 68.2,291 68.2,221"/>
    <polygon points="383.2,186 443.8,221 443.8,291 383.2,326 322.6,291 322.6,221"/>
    <polygon points="192.4,296.2 253,331.2 253,401.2 192.4,436.2 131.8,401.2 131.8,331.2"/>
    <polygon points="319.6,296.2 380.2,331.2 380.2,401.2 319.6,436.2 259,401.2 259,331.2"/>
  </g>
  <polygon points="256,186 316.6,221 316.6,291 256,326 195.4,291 195.4,221" fill="#ec5a52"/>
</svg>
```

- [ ] **Step 3: Afegir els enllaços PWA al `<head>` de `index.html`**

A `index.html`, just després de `<link rel="stylesheet" href="css/styles.css" />`, afegir:

```html
  <link rel="manifest" href="manifest.json" />
  <meta name="theme-color" content="#58b4c4" />
  <link rel="apple-touch-icon" href="icon.svg" />
```

- [ ] **Step 4: Verificar**

Run: `python -m http.server 8000` i obrir `http://localhost:8000`. A DevTools → Application → Manifest: ha d'aparèixer "Numerològic" amb la icona (mini-rusc, central coral). Comprovar que `icon.svg` es veu bé obrint `http://localhost:8000/icon.svg`. Aturar amb Ctrl+C.

- [ ] **Step 5: Commit**

```bash
git add manifest.json icon.svg index.html
git commit -m "feat: manifest PWA i icona mini-rusc"
```

---

## Task 5: Service worker `sw.js` (offline-first)

**Files:**
- Create: `sw.js`
- Modify: `js/app.js`

- [ ] **Step 1: Crear `sw.js`**

```javascript
// Service worker cache-first: app shell + puzzles.json. Offline-first.
const CACHE = "numerologic-v1";
const ASSETS = [
  "./",
  "./index.html",
  "./css/styles.css",
  "./js/app.js",
  "./js/game.js",
  "./js/ui.js",
  "./js/storage.js",
  "./js/modes.js",
  "./js/parser.js",
  "./js/evaluator.js",
  "./js/canonical.js",
  "./js/validate.js",
  "./js/score.js",
  "./js/share.js",
  "./data/puzzles.json",
  "./manifest.json",
  "./icon.svg",
];

self.addEventListener("install", (e) => {
  e.waitUntil(
    caches.open(CACHE).then((c) => c.addAll(ASSETS)).then(() => self.skipWaiting())
  );
});

self.addEventListener("activate", (e) => {
  e.waitUntil(
    caches.keys()
      .then((keys) => Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener("fetch", (e) => {
  if (e.request.method !== "GET") return;
  e.respondWith(
    caches.match(e.request).then((cached) =>
      cached ||
      fetch(e.request).catch(() => {
        if (e.request.mode === "navigate") return caches.match("./index.html");
      })
    )
  );
});
```

- [ ] **Step 2: Registrar el SW a `js/app.js`**

A `js/app.js`, al final del fitxer (després de `start(dailyIdx());`), afegir:

```javascript
if ("serviceWorker" in navigator) {
  navigator.serviceWorker.register("sw.js").catch(() => {});
}
```

- [ ] **Step 3: Verificar sintaxi**

Run: `node --check sw.js` i `node --check js/app.js`
Expected: cap sortida (sintaxi vàlida) en tots dos.

- [ ] **Step 4: Verificació manual offline**

Run: `python -m http.server 8000` i obrir `http://localhost:8000`.
1. DevTools → Application → Service Workers: `sw.js` ha d'estar "activated and running".
2. Application → Cache Storage → `numerologic-v1`: conté l'app shell i `puzzles.json`.
3. Activar **Offline** (Network → Offline) i **recarregar**: el joc segueix carregant i jugant.
4. (Opcional) "Install" / "Add to Home screen" disponible.
Aturar amb Ctrl+C.

- [ ] **Step 5: Commit**

```bash
git add sw.js js/app.js
git commit -m "feat: service worker offline-first (app shell + puzzles.json)"
```

---

## Task 6: Suite i tancament

- [ ] **Step 1: Executar tota la suite JS**

Run: `node --test` (des de l'arrel)
Expected: tots els tests passen (2a + 2b + share + els robustesa). Compte aproximat: 58.

- [ ] **Step 2: Verificació manual completa de la PWA**

Repetir la verificació offline de la Tasca 5 (Step 4) i el compartir de la Tasca 3 (Step 5). Confirmar: instal·lable, funciona offline, i el text de compartir té el format correcte sense cap solució.

- [ ] **Step 3: Completar la branca**

Announce: "I'm using the finishing-a-development-branch skill to complete this work."
**REQUIRED SUB-SKILL:** Use superpowers:finishing-a-development-branch — verificar tests, presentar opcions i, en triar PR, obrir-lo cap a `main` al repo `skinnydkd/numerologic`.

---

## Self-Review

- **Cobertura de la spec (`2026-05-25-share-pwa-design.md`):**
  - §2 `share.js` (`emojiBar`, `buildShareText`, format sense URL) → Task 1. ✓
  - §3 mecanisme de compartir (`navigator.share` / porta-retalls + "Copiat!") → Task 3. ✓
  - §4 `manifest.json` + enllaços `<head>` → Task 4. ✓
  - §5 `sw.js` cache-first (app shell + puzzles.json, versionat, activate neteja, navigate fallback) + registre → Task 5. ✓
  - §6 icona mini-rusc `icon.svg` → Task 4. ✓
  - §7 robustesa de `localStorage` (storage + game) → Task 2. ✓
  - §8 proves (share.js + storage corrupte amb node:test; PWA manual) → Tasks 1, 2, 5, 6. ✓
- **Placeholders:** cap; tot pas amb codi té el codi complet (inclòs l'SVG de la icona i el SW).
- **Consistència de tipus/noms:** `buildShareText({number, rankName, found, total, tuttiFound})` i `emojiBar(found, total)` coherents entre test, `share.js` i el handler d'`app.js`. El handler usa `game.rank()`, `game.found.size`, `game.puzzle.solutions.length`, `game.tuttiFound`, `poolIndex` — tots existents al Pla 2b. La llista `ASSETS` del SW cobreix tots els mòduls existents (2a + 2b + share). El paràmetre de restauració de `game.js` és `savedProgress` (fixat al 2b). ✓
```
